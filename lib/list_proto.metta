; Prototype umbrella for CeTTa list design experiments.
;
; Current split:
; - list: fast normal list surface backed by expressions
; - clist: explicit cons-list surface for inductive semantics

!(import! &self list)
!(import! &self clist)
